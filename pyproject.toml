[project]
name = "infra-sentinel"
version = "2.0.0"
description = "LLM-in-the-loop infrastructure watchdog: polls Zabbix + Prometheus, alerts in seconds from raw facts, then investigates with a self-hosted LLM. The model is never on the critical path of the notification."
readme = "README.md"
requires-python = ">=3.10"
license = { text = "MIT" }
authors = [{ name = "Marcelo Semino" }]
dependencies = []  # runtime is pure standard library on purpose

[project.optional-dependencies]
dev = ["pytest>=8", "ruff>=0.6"]

[project.scripts]
sentinel = "sentinel.cli:main"
sentinel-deadman = "sentinel.deadman:main"

[tool.setuptools.packages.find]
include = ["sentinel*"]

[tool.ruff]
line-length = 100
target-version = "py310"

[tool.ruff.lint]
select = ["E", "F", "I", "B", "UP", "S"]
ignore = ["S101"]  # asserts are fine in tests

[tool.ruff.lint.per-file-ignores]
"tests/*" = ["S", "E501"]
"mocks/*" = ["S"]

[tool.pytest.ini_options]
testpaths = ["tests"]
